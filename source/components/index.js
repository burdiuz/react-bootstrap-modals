import Modal from './Modal';
import BaseModal, {
  ModalCloseButton,
  ModalFooter,
  ModalHeader,
} from './BaseModal';
import ModalsHost, { getModalHostRef } from './ModalsHost';

export {
  BaseModal,
  ModalsHost,
  getModalHostRef,
  ModalCloseButton,
  ModalFooter,
  ModalHeader,
};

export default Modal;
